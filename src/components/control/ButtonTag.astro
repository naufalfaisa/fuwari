---
interface Props {
	size?: string;
	dot?: boolean;
	href?: string;
	label?: string;
}
const { dot, href, label }: Props = Astro.props;
---
<!-- Style 1 (default)-->
<!-- <a href={href} aria-label={label} class="btn-regular h-8 text-sm px-3 rounded-lg">
    {dot && <div class="h-1 w-1 bg-[var(--btn-content)] dark:bg-[var(--card-bg)] transition rounded-md mr-2"></div>}
    <slot></slot>
</a> -->

<!-- Style 2 -->
<!-- <a
  href={Astro.props.href}
  class="px-3 py-1 rounded-lg text-sm bg-[var(--btn-regular-bg)] hover:bg-[var(--btn-regular-bg-hover)] text-[var(--btn-content)] transition break-words">
  <slot />
</a> -->

<!-- Style 3 -->
<a href={href} class="btn-regular h-8 px-3 rounded-lg flex items-center overflow-hidden">
  {dot && <div class="h-1 w-1 mr-2 flex-shrink-0 bg-[var(--btn-content)] dark:bg-[var(--card-bg)] rounded-md"></div>}
  <span class="truncate"><slot></slot></span>
</a>

